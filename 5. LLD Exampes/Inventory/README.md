Inventory Management System (Java)

Simple in-memory inventory demo.

Compile:
javac -d bin Src/**/*.java

Run:
java -cp bin Inventory.Src.Main

This project uses a simple package layout matching the file tree under `Src/`.
